{
  "kind": "build_request",
  "title": "Integrate Google Places Autocomplete for Dubai location/building suggestions",
  "projectName": "OfficerRentDubai",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-34",
      "text": "Add Google Places Autocomplete API support to provide live location/building name suggestions for the public search Location input, restricted to UAE (components=country:AE) and biased toward Dubai, using the user’s typed input.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-user-1"
        ],
        "quotes": [
          "Google Places Autocomplete API. we can add this api"
        ]
      },
      "acceptanceCriteria": [
        "When a user types in the public Location input, the app fetches autocomplete suggestions from Google Places Autocomplete API using the current input value.",
        "The request restricts results to UAE via `components=country:AE` and applies a Dubai bias (e.g., location/radius or other supported request parameters) so suggestions prioritize Dubai locations/buildings.",
        "Suggestions are displayed in the existing typeahead dropdown and remain selectable by mouse and keyboard (ArrowUp/ArrowDown/Enter/Escape) as currently implemented.",
        "Selecting a Google suggestion populates the Location input with the selected suggestion text.",
        "If the Google API returns an error or no results, the UI fails gracefully without breaking search input behavior."
      ]
    },
    {
      "id": "REQ-35",
      "text": "Introduce a configurable Google Places API key for the frontend (do not hardcode secrets in source). If the key is missing at runtime/build time, show a clear English fallback behavior and avoid making failing requests.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-user-1"
        ],
        "quotes": [
          "Google Places Autocomplete API. we can add this api"
        ]
      },
      "acceptanceCriteria": [
        "The Google Places API key is read from a frontend configuration mechanism (e.g., a build-time environment variable) rather than being hardcoded in the repository.",
        "If the API key is not configured, the Location autocomplete does not spam failing network calls; instead it either (a) falls back to existing backend-powered suggestions (stored properties) or (b) disables autocomplete and shows a clear English message indicating autocomplete is unavailable until configured.",
        "All user-facing text introduced for this state is in English."
      ]
    },
    {
      "id": "REQ-36",
      "text": "Keep (or add) a backend-provided autocomplete fallback using the existing `getAutocompleteSuggestions(input, maxResults)` method so the public search location typeahead can still work when Google is unavailable, and ensure the frontend can switch between Google-based suggestions and backend-based suggestions safely.",
      "target": "both",
      "source": {
        "messageIds": [
          "msg-user-1"
        ],
        "quotes": [
          "Google Places Autocomplete API. we can add this api"
        ]
      },
      "acceptanceCriteria": [
        "Backend continues to provide `getAutocompleteSuggestions(input, maxResults)` based on stored properties’ `location` and `title`, deduplicated case-insensitively, and limited to the requested max results.",
        "Frontend suggestion logic uses Google Places suggestions when configured and healthy, but can fall back to backend suggestions on missing key, errors, or request failure, without changing the existing search submission behavior.",
        "No breaking changes are introduced to property search/filtering; clicking the Search button still applies the selected/entered Location value exactly as shown in the input."
      ]
    }
  ],
  "constraints": [
    "Do not edit files under frontend/src/components/ui (read-only UI primitives).",
    "Do not edit frontend immutable paths: frontend/src/hooks/useInternetIdentity.ts, frontend/src/hooks/useInternetIdentity.tsx, frontend/src/hooks/useActor.ts, frontend/src/main.tsx.",
    "Backend must remain a single Motoko actor in backend/main.mo; only add migration.mo if an actual stable-state upgrade is required."
  ],
  "nonGoals": [
    "Do not implement full Google Maps rendering, map widgets, geolocation, or place details pages.",
    "Do not attempt to download or store a complete offline list of all Dubai buildings (use live autocomplete only).",
    "Do not change the property schema or listing search filters beyond what is needed to support Google autocomplete suggestions."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  },
  "userProfileUpdate": {
    "goalsToAdd": [],
    "goalsToRemove": [],
    "preferencesToAdd": [],
    "preferencesToRemove": [],
    "miscToAdd": [],
    "miscToRemove": [],
    "fieldsToSet": {},
    "fieldsToDelete": []
  }
}